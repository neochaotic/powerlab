# govulncheck allowlist — reachable (called) CVEs we knowingly accept FOR NOW.
#
# scripts/govulncheck-gate.sh blocks CI on any reachable, non-stdlib vuln that
# is NOT listed here. This file is the ratchet: the gate catches NEW regressions
# (the next viper-class boot break) while the known backlog is tracked below.
# Shrink this list as remediations land; never grow it without a tracking issue.
#
# Format: one OSV id per line (first whitespace-delimited token). Everything
# after is a comment: module — reason — tracking.
#
# Stdlib CVEs are NOT listed here: the gate ignores them by design (they are
# handled by keeping the Go toolchain current via CI's floating go-version).

# ── moby/docker stack — residual after the docker v28 / compose-go v2
#    migration (#523, landed on this branch). The migration cleared runc,
#    buildx, otelgrpc, containerd GO-2025-3528, docker GO-2025-3829, compose
#    GO-2025-4077. What remains: docker/docker has NO upstream fix; buildkit/
#    grpc/containerd-v2/compose/otel still ship vulnerable in compose v2.40.2's
#    pinned set and clearing them needs a further bump that cascades into Go
#    1.26 — tracked as a follow-up under #523.
GO-2026-4887  # docker/docker — no upstream fix — #523
GO-2026-4883  # docker/docker — no upstream fix — #523
GO-2026-4859  # moby/buildkit — needs v0.28.1 (Go 1.26 cascade) — #523
GO-2026-4858  # moby/buildkit — needs v0.28.1 (Go 1.26 cascade) — #523
GO-2026-4762  # google.golang.org/grpc — needs v1.79.3 (Go 1.26 cascade) — #523
GO-2025-4108  # containerd/v2 — needs newer (Go 1.26 cascade) — #523
GO-2025-4100  # containerd/v2 — needs newer (Go 1.26 cascade) — #523
GO-2026-4610  # docker/compose/v2 — needs newer compose (Go 1.26 cascade) — #523
GO-2026-4394  # go.opentelemetry.io/otel/sdk — pulled by compose v2.40.2 — #523

# ── no upstream fix available (advisory marks Fixed: N/A). Reassess on each
#    govulncheck run; remove if/when a fixed release ships.
GO-2025-4020  # nwaples/rardecode — no upstream fix — #518

# ── rclone (local-storage): reachable by static call-graph but NOT exposed.
#    local-storage uses rclone only as a mount library (cmd/mountlib/fs/vfs);
#    it never starts the rc/rcd remote-control daemon (GO-2026-4964's attack
#    surface) nor the --links --metadata sync path (GO-2024-3271). Bump to
#    >=1.73.5 tracked in #525.
GO-2026-4964  # rclone — rc options/set auth-bypass; rcd not started — bump #525
GO-2024-3271  # rclone — symlink perms (--links --metadata); unused path — bump #525

# ── other reachable deps with an independent fix path (not moby-coupled).
#    Candidates for opportunistic bumps; tracked under the #518 umbrella.
GO-2025-3605  # mholt/archiver/v3 — independent bump candidate — #518
GO-2024-2698  # mholt/archiver/v3 — independent bump candidate — #518
GO-2025-3533  # getkin/kin-openapi — independent bump candidate — #518
GO-2023-1989  # golang.org/x/image (core) — independent bump candidate — #518
GO-2023-1990  # golang.org/x/image (core) — independent bump candidate — #518
GO-2024-2937  # golang.org/x/image (core) — independent bump candidate — #518
GO-2026-4815  # golang.org/x/image (core) — independent bump candidate — #518
