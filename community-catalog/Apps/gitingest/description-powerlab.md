Gitingest takes a GitHub repository URL and produces a single text dump — flattened source files with directory structure prefix — ready to paste into a long-context LLM.

Handy if you want to ask Claude or ChatGPT "what does this codebase do" without uploading 200 files manually. Runs locally so private repos stay private.
