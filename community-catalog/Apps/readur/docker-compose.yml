version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: readur_app_1
      APP_PORT: 8000

  app:
    image: ghcr.io/readur/readur:2.9.1@sha256:16aa8b1d7df218ab0f4fc57fbf890d125020fca416bc3555aa7731e7329020dc
    user: "1000:1000"
    restart: on-failure
    environment:
      DATABASE_URL: postgresql://readur:readur_password@readur_postgres_1:5432/readur
      JWT_SECRET: ${APP_SEED}
      SERVER_ADDRESS: 0.0.0.0:8000
      UPLOAD_PATH: /app/uploads
      WATCH_FOLDER: /app/watch
      ALLOWED_FILE_TYPES: pdf,png,jpg,jpeg,tiff,bmp,gif,txt,rtf,doc,docx
      WATCH_INTERVAL_SECONDS: 30
      FILE_STABILITY_CHECK_MS: 500
      MAX_FILE_AGE_HOURS: 168
      OCR_LANGUAGE: eng
      CONCURRENT_OCR_JOBS: 4
      OCR_TIMEOUT_SECONDS: 300
      MAX_FILE_SIZE_MB: 50
      MEMORY_LIMIT_MB: 512
      CPU_PRIORITY: normal
      ADMIN_PASSWORD: readur2024
    volumes:
      - ${APP_DATA_DIR}/data/uploads:/app/uploads
      - ${APP_DATA_DIR}/data/watch:/app/watch
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:17.5@sha256:3962158596daaef3682838cc8eb0e719ad1ce520f88e34596ce8d5de1b6330a1
    user: "1000:1000"
    restart: on-failure
    environment:
      POSTGRES_USER: readur
      POSTGRES_PASSWORD: readur_password
      POSTGRES_DB: readur
    volumes:
      - ${APP_DATA_DIR}/data/postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U readur"]
      interval: 10s
      timeout: 5s
      retries: 5

x-powerlab:
    store_app_id: readur
    title:
        en_us: Readur
    tagline:
        en_us: An intuitive OCR platform for documents
    description:
        en_us: "Readur \U0001F4C4 Readur is a powerful and modern document management system designed to help individuals and teams efficiently organize, process, and access their digital documents. It combines a high-performance backend with a sleek and intuitive web interface to deliver a smooth and reliable user experience. You can check our our docs at docs.readur.app. ✨ Features | Feature | Description | Documentation | |---------|-------------|---------------| | \U0001F510 **Secure Authentication** | JWT-based user authentication with bcrypt password hashing + OIDC/SSO support | User Management, OIDC Setup | | \U0001F465 **User Management** | Role-based access control with Admin and User roles | User Management Guide | | \U0001F4E4 **Smart File Upload** | Drag-and-drop support for PDF, images, text files, and Office documents (DOCX, XLSX, DOC*) | File Upload Guide | | \U0001F50D **Advanced OCR** | Automatic text extraction using Tesseract and Office document parsing | OCR Optimization | | \U0001F30D **Multi-Language OCR** | Process documents in multiple languages simultaneously with automatic language detection | Multi-Language OCR Guide | | \U0001F50E **Powerful Search** | PostgreSQL full-text search with multiple modes (simple, phrase, fuzzy, boolean) | Advanced Search Guide | | \U0001F517 **Multi-Source Sync** | WebDAV, Local Folders, and S3-compatible storage integration | Sources Guide, S3…"
    icon: https://getumbrel.github.io/umbrel-apps-gallery/readur/icon.svg
    category: files
    port_map: "8111"
    author: Jon Fuller
    developer: Jon Fuller
    source:
        catalog: umbrel-apps
        upstream_id: readur
        upstream_repo: https://github.com/getumbrel/umbrel-apps.git
        upstream_commit: 0299c400450d07fee62d3dcb0e6c24b00f359b51
        upstream_path: readur/umbrel-app.yml
        transform_version: "1.0"
        synced_at: "2026-05-12T03:53:15Z"
