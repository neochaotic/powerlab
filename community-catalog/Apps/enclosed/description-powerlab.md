Enclosed lets you share a one-off note (text or file) via a URL that the recipient can open once before it self-destructs. Encryption happens in the browser — your PowerLab box never sees the plaintext.

Think privnote.com but on hardware you control. Useful for sharing credentials, sensitive snippets, or anything you'd otherwise hand off via Slack DM.
